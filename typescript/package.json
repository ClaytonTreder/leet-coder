{
  "name": "leet-coder-typescript",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=json --outputFile=vitest-results.json",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "new": "node scripts/new-problem.mjs"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
